ToDo:
1.  SigmaPoint{Tuple{UvGaussian...}} is another type of SigmaPoints
    -   Tuple must consist of UvGaussian
    -   Can build a "getindex" function for this type of sigma points
    -   Extract mean from each value 
    -   Add/subtract std to the right element according to the index (using @reset from Accessors)
2.  Create a method "gaussian" that produces a 
        -   UvGaussian for SigmaPoints{AbstractVector{<:Number}}
        -   MvGaussian for SigmaPoints{AbstractVector{<:AbstractVector}}
    -   If functions produce a scalar, we get a UvGaussian
    -   This doesn't require a chol_update, just add weights times the points and take the square root
    -   Either way we still need vector form SigmaPoints 
        -   We need to calculate its mean before doing the standard deviation
        -   This means every sigma point gets used twice (so we need to store it in memory)
        -   The UvGaussian function arguments can produce an SVector{2*N+1}
3.  Univariate sigma point ops on known functions can use pre-canned parameters
    -   This is a good resource to verify other scaling rules
        -   https://www.mathworks.com/help/ident/ug/extended-and-unscented-kalman-filter-algorithms-for-online-state-estimation.html
    -   Non-asymptotic functions should have alpha=1
        x*y, sin(x), cos(x), exp(x)
    -   Asymptotic functions should have alpha scaled such that it doesn't go too close to zero 
        -   Locate the nearest asymptote and designate it as an upper or lower bound
        -   The lower bound should be LB + max(1, (mu-LB)/sig)
            -   If the asymptote is at 0 x has mean of 0.1 and std of 2.0, the lower bound should be 0.05
        -   The upper bound should be UB - max(1, (UB-mu)/sig)
        -   Each function should have an "asymptote" function with it 
            -   asymptotes(typeof(inv), UvGaussian) = 0 #Check scaling against a single asymptote
            -   asymptotes(typeof(sin), UvGaussian) = nothing
            -   asymptotes(typeof(asin), UvGausian) = (0,1) #check all scaling and return minimum
        -   scale_asymptote(x::UvGaussian, z)
    -   
    -   inv(x) must have sigma points that don't go more than halfway to zero (alpha can be 1 but can be less)
    -   x/y should be x*inv(y)
    -   tan(x) should be sin(x)*inv(cos(x))


(future)
1.  Instead of being "yet another Kalman filter", this should be an unscented version of "Measurements.jl"
    -   "A second-order approximation of uncertainty without dependencies on derivatives"
    -   Kalman filtering functions shouldn't even be automatically exported
    -   "UvGaussian" should be the old "Measurement" object
        -   "UvGaussian{T<:Real}" shoudl be an "AbstractGaussVar"
        -   "MvGaussian{V<:AbstractVector, M<:Cholesky}" should be an "AbstractGaussVec"
        -   Both are the unexported "AbstractGaussian"
    -   Otherwise keep main points of the Measurements.jl API
        -   Use the "±" notation to construct and display UvGaussian
            -   Can also be used to construct MvGaussian
        -   Use Statistics functions (mean, std, cov) instead of "value" and "uncertainty"
            -   std and var on MvGaussian produces a vector (maintain the same type as mean)
        -   Support a modified version of the @uncertain macro
            -   Unscented transform must be the first argument
        -   We may need underscore methods to reduce confusion with Statistics 
            -   For example _std(x::Float64) should be zero (in Statistics, it's NaN)
    -   Interop between UvGaussian and MvGaussian
        -   MvGaussian on a vector of UvGaussian produces an MvGaussian with a diagonal Cholesky
        -   scalar indexing MvGaussian produces UvGaussian
        -   vector indexing MvGaussian produces a downsampled MvGaussian
    -   FlexUnits extension

2.  Create a new repository called TagMeasurements.jl
    -   This requires FlexUnits, TimeRecords, UnscentedTransforms as dependencies

    -   Main object "TagMeas{T,D}" and should have the following fields:
        -   name::String, value::RefValue{T}, stdev::RefValue{T}, reltol::T, abstol::T, units::Units{D, AffineTransform{Float64}},
                bounds::SVector{2,T}, aggregation::Symbol, history::TimeSeries{T}, indhint::RefValue{Int}
        -   include "abstol" and "reltol" fields that can be "nan"
        -   "stdev" should be nanmax(m.abstol + m.reltol*m.value[])

    -   Define the API for AbstractMeas and set "TagMeas" as one of the objects in this hierarchy 
        -   readhistory!, rawvalue, rawstdev, rawgauss, sivalue, sistdev, sigauss, quantvalue, quantstdev, quantgauss

